<% branch_view = "forms/edit" %>

<%= render partial: "master_decks/common/breadcrumbs", locals: { master_deck: @master_deck, selected_branch: @branch, show_full_breadcrumbs: @show_full_breadcrumbs } %>

<%= render partial: "master_decks/common/main_deck_nav", locals: { master_deck: @master_deck } %>

<%= render partial: "master_decks/deck_stats", locals: { master_deck: @master_deck, branch: @branch } %>

<%= render partial: "master_decks/common/branch_card", locals: { master_deck: @master_deck, branch: @branch, 
                                                                  current_branches: @current_branches, branched_from_id: @branched_from_id, 
                                                                  branch_view: branch_view } %>

